{
  "name": "lorecal",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "install:react": "cd client && npm install --production",
    "gcp-build": "npm run-script install:react",
    "build": "npm run build --prefix ./client",
    "start": "node ./bin/www & npm start --prefix ./client"
  },
  "dependencies": {
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "express": "~4.16.0",
    "morgan": "~1.9.0",
    "node-sass-middleware": "0.11.0"
  }
}
